language: python
dist: bionic
services:
  - mysql
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS users;'
install:
  - pip install -r requirements.txt
script:
  - python -m unittest tests/users_tests.py
  - coverage run --source=services -m unittest discover -s tests/
  - coverage report

